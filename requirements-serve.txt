# Extra dependencies for the web UI (serve.py)
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-serve.txt
gradio>=4.0.0

# LLM Enhancement backends (pre/post-processing) — all lightweight, no GPU needed
# Imported lazily: unused backends add zero overhead at runtime
anthropic>=0.40.0
google-generativeai>=0.8.0
openai>=1.50.0
